{
  "version": 2,
  "builds": [
    {
      "src": "web/**",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "web"
      }
    },
    {
      "src": "api/**",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/index.py"
    },
    {
      "src": "/(.*)",
      "dest": "/web/index.html"
    }
  ]
}